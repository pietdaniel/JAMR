from User import User 
from Room import Room 


def createUser(user):
  user['inst']
  user['genr']
  user['uid']
  user['pos']['lon']
  user['pos']['lat']

def createRom(room):
  users = room['users']
  room['uid']
  for user in users: 
    user['uid']

